// duplicate.validator.js

const duplicateValidator = async (curriculumId) => {

    // validation logic

    return {
        valid: true,
        errors: [],
        warnings: [],
    };

};

module.exports = duplicateValidator;